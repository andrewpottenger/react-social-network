import { dimension } from './variables'

export const mainContainer = {
  maxWidth: dimension.mainContianerMaxWidth,
  margin: '0 auto',
}

export const headerContainer = {
  maxWidth: dimension.headerContainerMaxWidth,
  margin: '0 auto',
}
